vO.1提示词
我要开发一个「写作 Agent」产品。需求如下:
产品功能:用户通过对话框输入写作主题，上传风格参考文章(或提供文件路径)，指定输出路径。Agent 自主完成从调研到成稿的全流程，最终在输出路径下生成文稿。

#交互方式:
用户在对话界面输入:「帮我写一篇关于 XX的文章，风格参考见 XX 文件，输出到XX 目录」。Agent 执行过程中，用户可以查看当前进度(如有UI)，也可以中途追加指示。任务完成后，Agent告知用户文稿路径。

#技术方案:
基于LLM API，实现一个Agent循环:接收用户需求模型推理决定是否调用工具调用工具拿到结果继续推理，直到模型判断任务完成。
提供以下工具供模型调用:
bash (执行 shell命令)、
read_file (读取文件)、
wnite_file: (写入文件)、
edit_file(编辑文件)、
web_search(联网搜索)。
每轮循环中，模型自行决定下一步是搜索资料、阅读参考文章、撰写文稿还是编辑修改。

# 预期行为:
Agent 自主完成「调研-构思-撰写-修改」全流程，无需用户中途干预。最终在输出路径下生成完成的文稿。


v0.2：任务规划与状态管理 —— 让 Agent 知道自己在干嘛
## 任务规划机制:
Agent收到用户需求后，在进入循环之前，必须先制定一份结构化的任务计划。
任务计划以 markdown 文件的形式保存在输出路径下(plan.md)，包含步骤清单和当前状态。计划中每一步是一个checkbox 项，
格式如:
-[]步骤描述。
完成的步骤标记为 -[x]计划还包含一个「当前状态」区域，标注当前执行到第几步、备注信息等。

#状态管理机制:
Agent 每完成一个步骤，必须先更新 plan.md(打勾+更新当前状态)，再继续下一步。
当所有步骤都打勾后，Agent 做一次最终检查，确认文稿已写入输出路径，然后结束任务。

#熔断机制:
设置最大循环轮次为50轮。超过50轮强制停止，输出当前已有成果。

## 预期效果:
通过强制任务规划和状态追踪，让Agent 在长任务中始终保持「知道自己做到哪了」，减少跑偏、重复、遗漏的发生。


v0.3：上下文管理 —— 在有限空间里「无限」工作
## 上下文压缩策略:

-当Agent 的消息历史(所有对话轮次的总token数)接近上下文窗口的70% 时，系统自动触发压缩。
压缩方式:调用一个独立的LLM 请求，将早期对话轮次(已完成步骤的工具调用细节)压缩为摘要，替换原始消息。
压缩后的摘要中保留:关键结论、决策理由、文件路径引用。丢弃:搜索结果原文、中间推理过程、已归档文件的完整内容。
被压缩的原始内容写入输出路径下的 .archive目录，Agent 需要时可以重新读取。
# Subagent 支持:
搜索环节支持派生子Agent(Subagent)。主Agent 发出搜索指令，Subagent在独立的上下文中执行搜索、筛选，只返回结论摘要给主 Agent。
好处:10条搜索x每条3000字的原始结果，压缩为 10条x每条50字的摘要，主Agent上下文占用从 30000字降到500字。
## 工作记录机制:
Agent 每完成一个步骤，将该步骤的工作记录(做了什么、关键结论、生成了哪些文件)写入输出路径下的work_log.md
后续步骤需要回顾之前的结果时，从 Work_1og 读取，而不是依赖对话历史中的原始内容。
# 技能按需加载:
如果Agent 内置了多种写作技能模板(如学术写作、公众号写作、商业文案)，系统提示词中只放技能目录(名称+一句话描述)。
- 当模型判断需要某种技能时，通过工具调用加载该技能的完整说明，注入当前上下文。
# 预期效果:
通过压缩、分流、按需加载等策略，让Agent 在有限的上下文窗口内完成长链路任务，不会因为信息撑爆窗口而中断。

v0.4：沙箱与权限管理 —— 给 Agent 画一个安全围栏

## 工作空间沙箱:
Agent 的所有文件操作必须限制在用户指定的输出路径内。
系统在每次执行文件操作前，自动将路径解析为绝对路径并校验是否在工作空间内，越界则拒绝执行并返回错误信息给Agent。

-工作空间即输出路径，Agent 启动时自动确认该路径存在且可写。
##命令分层权限:
白名单命令(直接执行,无需用户确认):ls、cat、mkdir、grep、head、tail、wc、pwd
需确认命令(执行前弹窗询问用户):curl、wget(下载外部资源)、rm(删除文件)、mv(移动文件)
黑名单命令(永远拒绝):rm -rf/、chmod777、sudo、任何涉及系统目录的操作

# 路径逃逸防护:
-Agent 在多次目录跳转中可能拼接出../..这样的路径意外逃出工作空间。系统在每次文件操作前，将路径规范化(resolve)后再做校验。
##联网内容管理:
-联网下载的内容(如搜索结果、网页正文)必须保存到输出路径下的 sources/子目录，不得写入其他位置。
# 预期效果:
通过沙箱和权限分层，确保 Agent 的活动范围可控，即使模型推理出错也不会造成破坏性操作。


v0.5：Hooks —— 不需要模型操心的自动化
## Hooks机制:
在Agent 循环的关键节点，插入自动执行的脚本(Hooks)，不经过模型推理，由程序直接执行。
需要实现以下 Hooks:
## 执行前 Hooks(工具调用之前触发):
路径校验Hook:对所有文件操作的路径参数做安全校验(解析绝对路径检查是否在工作空间内越界则拦截并返回错误)。
JS0N 格式校验Hook:检查模型生成的工具调用参数格式是否正确(如缺少引号、括号不匹配等)，可自动修复的就修复，修复不了的才回传给模型重新生成。
# 执行后 Hooks(工具调用之后触发):
任务状态注入Hook:每轮循环结束时，自动读取pLan.md 的最新内容，拼接到消息列表末尾。利用近因效应确保模型每轮都能看到「我做到哪了」。
工作日志Hook:每次‘write file/ editfile 执行成功后，自动将文件变更记录追加到‘wonk 1og.md进度监控Hook:每轮循环结束时，检查 pLan.md 的最后更新时间。如果连续3 轮未更新，向消息列表注入一条提醒:「你已经3轮没有更新任务计划了，请审视当前进度。」
-上下文压缩Hook:每轮循环结束时，检查消息总长度。超过上下文窗口的70%时，自动触发上下文压缩流程。
## 预期效果:
通过Hooks将机械性操作从模型的注意力中剥离出来，模型专注于写作本身，后台管家负责状态维护、安全检查和进度监控。



v0.6：记忆管理 —— 让 Agent 越用越好
## 记忆系统:
记忆提取:每次任务结束后，系统自动调用LLM，从本次对话中提取值得长期记忆的信息，包括:用户偏好的写作风格、常写主题、被纠正过的写作习惯、常用输出路径等。提取结果以结构化文本存储。首轮注入:每次新会话开始时，系统自动将用户记忆摘要注入到首轮上下文的系统提示词区域。Agent 在写作过程中应遵循这些偏好，但如果用户在本次对话中给出冲突指示，以本次为准。-记忆更新:每次任务结束后，新的提取结果与已有记忆合并(去重、更新、淘汰过时信息)。
#跨会话检索:
当用户在新会话中提到「上次写的那篇XX」「之前我们讨论过的 XX」等涉及历史会话的内容时，Agent可通过关键词检索历史会话记录。
检索结果不是全量加载，而是只拉取与关键词相关的片段摘要，注入当前上下文。
## 预期效果:
Agent 从「每次见面都是陌生人」进化为「越用越懂你的写作助手」。用户无需每次重复自己的偏好，历史产出也可被复用和引
用。


v0.7：错误恢复 —— 小错不崩，大错可控

##错误恢复机制:
#错误结构化处理:
工具调用失败时，不把原始错误日志直接丢给模型。系统自动将错误处理为结构化摘要，包含:失败的工具名、失败原因(超时/权限/格式等)、建议的下一步操作(重试/换策略/跳过)。
-结构化错误信息比一堆stack trace 对模型友好得多，模型看到后知道该怎么做。

##失败计数与降级策略:
对每个工具的每次调用维护失败计数。
第1次失败:将结构化错误信息返回给模型，让它自行决定是否重试。
第2次失败:在错误信息中明确建议「换策略或跳过该步骤」。
第3次及以上失败:强制跳过该步骤，在plan.md 中标记为「已跳过」并记录原因，继续执行后续步骤。

##自动存档与回滚:
-每次 write_file 或edit_file 执行成功后，系统自动保存一份快照(如通过 git commit 或文件备份)。

- 如果后续步骤导致文稿损坏，Agent 或用户可以回滚到上一个正确版本。

预期效果:
通过结构化错误处理、渐进式降级和自动存档，确保Agent 在面对工具调用失败时「小错不崩、大错可控」，不会因为一次失败就导致整个任务报废。